## A Log Clean up tool

To avoid excessive disk usage, this script delete logs older than 30 days

- Uses `find` command to find the log file and deletes it
- This reduces memory/disk usages of the system.